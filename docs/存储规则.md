# 存储规则

- **统一模型**:local 和 s3 一律按远程慢存储对待,接口全异步;写入以整文件替换为单位,不做部分更新。
- **读**:允许任意并发,不加锁。
- **写**:同一文件串行,写队列(写锁)放在 Worker 内存;同一浏览器共享同一个 Worker,因此跨标签页也串行;独立浏览器之间无法保证。
- **覆盖**:独立浏览器之间先读后写的时间差可能导致覆盖(最后写入者胜),不检测、不避免。理由:检查本身也有时间差,无法消除,因此不管。
